kind = "web"
previewPath = "/"
title = "RummyApp"
version = "1.0.0"
id = "artifacts/rummy-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25629

[services.development]
run = "pnpm --filter @workspace/rummy-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/rummy-app", "run", "build" ]
publicDir = "artifacts/rummy-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25629"
BASE_PATH = "/"
